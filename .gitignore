.venv/
__pycache__/
*.pyc
transcripts/
models/
.superpowers/

# Design handoffs and review bundles live in the working tree but are not part
# of the app. Listed explicitly after a `git add -A` swept them into a commit
# and pushed them to a public remote — see the untracking commit for the story.
design_handoff_projector_display/
*.zip
